akka {
  loggers = [akka.event.slf4j.Slf4jLogger]
  loglevel = debug
  actor {
    debug {
      receive = off
      lifecycle = off
    }
  }
}

jedis {
  poolsize = 128
}

redis {
  hostname = "localhost"
  port = 6379
  geochannel = "geoevent"
}

spray {
  port = 8081
}